import { ShortFrenchDatePipe } from './short-french-date.pipe';

describe('ShortFrenchDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortFrenchDatePipe();
    expect(pipe).toBeTruthy();
  });
});
