import { SortingCitiesPipe } from './sorting-cities.pipe';

describe('SortingCitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingCitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
