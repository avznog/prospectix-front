import { FrenchDatePipePipe } from './french-date-pipe.pipe';

describe('FrenchDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FrenchDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
